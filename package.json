{
  "name": "jev-flowmap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tooling for the jev-flowmap Claude Code plugin. The plugin itself has no runtime dependencies.",
  "scripts": { "test": "node --test 'tests/*.test.js'" },
  "engines": { "node": ">=22" }
}
